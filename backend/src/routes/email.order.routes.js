/*import { Router }
from "express";

import {
    sendOrderEmail
}
from "../controllers/email.order.controller.js";

const router = Router();

router.post("/send-order-email", sendOrderEmail);

export default router;*/